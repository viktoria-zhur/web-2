from flask import Blueprint, render_template, jsonify, request

lab7 = Blueprint('lab7', __name__)

# Список фильмов (минимум 5)
films = [
    {
        "title": "Interstellar",
        "title_ru": "Интерстеллар",
        "year": 2014,
        "description": "Когда засуха, пыльные бури и вымирание растений приводят человечество к продовольственному кризису,<br>коллектив исследователей и учёных отправляется сквозь червоточину в путешествие,<br>чтобы превзойти прежние ограничения для космических путешествий человека<br>и найти планету с подходящими для человечества условиями."
    },
    {
        "title": "The Shawshank Redemption",
        "title_ru": "Побег из Шоушенка",
        "year": 1994,
        "description": "Бухгалтер Энди Дюфрейн обвинён в убийстве собственной жены и её любовника.<br>Оказавшись в тюрьме под названием Шоушенк, он сталкивается с жестокостью и беззаконием,<br>царящими по обе стороны решётки.<br>Каждый, кто попадает в эти стены, становится их рабом до конца жизни.<br>Но Энди, обладающий живым умом и доброй душой, находит подход как к заключённым,<br>так и к охранникам, добиваясь их особого к себе расположения."
    },
    {
        "title": "The Green Mile",
        "title_ru": "Зеленая миля",
        "year": 1999,
        "description": "Пол Эджкомб — начальник блока смертников в тюрьме «Холодная гора»,<br>каждый из узников которого однажды проходит «зеленую милю» по пути к месту казни.<br>Пол повидал много заключённых и надзирателей за время работы.<br>Однако гигант Джон Коффи, обвинённый в страшном преступлении,<br>стал одним из самых необычных обитателей блока."
    },
    {
        "title": "Fight Club",
        "title_ru": "Бойцовский клуб",
        "year": 1999,
        "description": "Терзаемый хронической бессонницей и отчаянно пытающийся вырваться из мучительно скучной жизни,<br>клерк встречает некоего Тайлера Дёрдена — харизматического торговца мылом с извращенной философией.<br>Тайлер уверен, что самосовершенствование — удел слабых,<br>а саморазрушение — единственное, ради чего стоит жить."
    },
    {
        "title": "Léon",
        "title_ru": "Леон",
        "year": 1994,
        "description": "Профессиональный убийца Леон, не знающий пощады и жалости,<br>знакомится со своей очаровательной соседкой Матильдой,<br>семья которой погибает от рук агента Управления по борьбе с наркотиками Стенса.<br>Девочке некуда идти, и Леон, сам того не желая, становится её защитником."
    }
]

@lab7.route('/')
def main():
    return render_template('lab7/index.html', films_count=len(films))

# Получение всех фильмов
@lab7.route('/rest-api/films/', methods=['GET'])
def get_films():
    return jsonify(films)

# Получение одного фильма по ID
@lab7.route('/rest-api/films/<int:id>', methods=['GET'])
def get_film(id):
    if id < 0 or id >= len(films):
        return jsonify({"error": "Фильм не найден"}), 404
    return jsonify(films[id])

# Удаление фильма по ID
@lab7.route('/rest-api/films/<int:id>', methods=['DELETE'])
def delete_film(id):
    if id < 0 or id >= len(films):
        return jsonify({"error": "Фильм не найден"}), 404
    films.pop(id)
    return '', 204

# Редактирование существующего фильма (PUT)
@lab7.route('/rest-api/films/<int:id>', methods=['PUT'])
def put_film(id):
    if id < 0 or id >= len(films):
        return jsonify({"error": "Фильм не найден"}), 404
    
    # Получаем новые данные из тела запроса
    new_film_data = request.get_json()
    
    # Обновляем фильм
    films[id] = new_film_data
    
    # Возвращаем обновленный фильм
    return jsonify(films[id])

# Добавление нового фильма (POST)
@lab7.route('/rest-api/films/', methods=['POST'])
def add_film():
    # Получаем данные нового фильма из тела запроса
    new_film_data = request.get_json()
    
    # Добавляем новый фильм в конец списка
    films.append(new_film_data)
    
    # Возвращаем индекс нового фильма (длина списка минус 1)
    return jsonify({"id": len(films) - 1}), 201